# Aspect Ratio Calculator

A simple browser tool to calculate and maintain aspect ratios for images, videos, and responsive layouts.

## Usage

Open `index.html` in a browser or publish this folder with GitHub Pages.

## Branching

Work happens on `dev`. Merge to `main` only after manual QA.
